<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dwinovo.ling_cloud.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="com.dwinovo.ling_cloud.pojo.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="nickname" property="nickname"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户名查找用户 -->
    <select id="findByUsername" resultMap="UserResultMap">
        SELECT id, username, password_hash, nickname, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>

    <!-- 根据ID查找用户 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT id, username, password_hash, nickname, create_time, update_time
        FROM user
        WHERE id = #{id}
    </select>

    <!-- 插入新用户 -->
    <insert id="insert" parameterType="com.dwinovo.ling_cloud.pojo.User">
        INSERT INTO user (id, username, password_hash, nickname, create_time, update_time)
        VALUES (#{id}, #{username}, #{passwordHash}, #{nickname}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新用户信息 -->
    <update id="update" parameterType="com.dwinovo.ling_cloud.pojo.User">
        UPDATE user
        SET username = #{username},
            password_hash = #{passwordHash},
            nickname = #{nickname},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

</mapper>